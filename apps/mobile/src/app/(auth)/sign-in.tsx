import { Screen, Text } from '../../ui';

/** Placeholder — real sign-in screen lands in Task 7. */
export default function SignIn() {
  return (
    <Screen>
      <Text variant="h2">Sign in (Task 7)</Text>
    </Screen>
  );
}
