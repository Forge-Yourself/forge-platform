import { Screen, Text } from '../../ui';

/** Placeholder — real MFA challenge screen lands in Task 9. */
export default function MfaChallenge() {
  return (
    <Screen>
      <Text variant="h2">MFA challenge (Task 9)</Text>
    </Screen>
  );
}
