import { Screen, Text } from '../../ui';

/** Placeholder — real PT profile setup screen lands in Task 10. */
export default function PtProfile() {
  return (
    <Screen>
      <Text variant="h2">PT profile setup (Task 10)</Text>
    </Screen>
  );
}
