import { Screen, Text } from '../../ui';

/** Placeholder — real role picker screen lands in Task 10. */
export default function Role() {
  return (
    <Screen>
      <Text variant="h2">Choose role (Task 10)</Text>
    </Screen>
  );
}
