# Repo-root secrets used by CLI work, not by any app at runtime.
# Copy to .env — .env is gitignored.

# Supabase dashboard -> Project Settings -> Database. Used by `supabase db push`.
SUPABASE_DB_PASSWORD=
